# Changelog

## Unreleased

- [修复] 生产方向恢复为严格编号顺序；当前方向任何阶段未闭环时立即暂停整个流程，禁止跳过失败方向或提前操作下一方向。
- [修复] 预览生成、语义分层和批量透明素材在主等待结束后增加边界回扫，避免遗漏刚好在超时边界返回的有效结果；语义分层额外等待 15 秒完整标记 JSON。
- [修复] 运行状态明确记录 `current_direction_incomplete` 阻塞原因、方向编号与失败阶段，便于后续只恢复当前方向。

## 1.7.4

- [调整] 拆图改为每个方向只向 ChatGPT 正式提交一次批量请求；请求中的每个复杂元素必须分别返回独立透明 PNG，禁止合图、逐元素追问和修复对话。
- [调整] 文字与数字必须保留为可编辑文本，普通功能图标必须使用 Remix Icon，背景、卡片、红包/信封背板、按钮和简单矩形等结构必须在 Figma 原生重绘。
- [移除] 停止从完整预览裁剪素材以及本地抠图、去背景、透明边裁切和 Alpha 修复；仅接收并原样保存 ChatGPT 已直接生成透明通道的素材。
- [新增] ChatGPT 返回素材少于请求数量时，保留所有已通过素材并允许 `partial` 方向进入 Figma；缺少关键复杂素材仍由最终 Figma 质检拦截。
- [兼容] 旧版拆图报告自动失效并按新策略重新拆图，避免恢复任务时继续沿用旧裁剪或本地抠图结果。

## 1.7.3

- [调整] 十张参考图仍可一次审核；生产阶段改为逐方向严格闭环。每个方向拆图完成即启动5分钟计时并发出 Figma 工作事件，只有当前方向通过 Figma QA 且计时结束后才进入下一方向。
- [调整] 五分钟等待期间复用并保持同一个 Chrome 会话空闲；原有审核聊天正式提交间隔继续保持30秒。
- [新增] 将拆图完成时间、冷却截止时间和当前方向闭环状态持久化到运行记录；恢复时沿用原时间，不重新计时。当前方向 Figma 失败时暂停而不是跳过。

## 1.7.2

- [修复] 生图、语义拆图和复杂素材补全统一将 ChatGPT 已保存对话接口限制为每 15 秒最多读取一次，同时保留不发请求的本地 DOM 高频检查；检测到对话访问限流后立即暂停整个运行，避免继续处理后续方向加重限制。
- [优化] 方向聊天重命名优先复用当前聊天侧栏中的菜单，只有侧栏找不到目标聊天时才回到项目页兜底，减少每个方向的整页往返刷新。
- [修复] 语义拆图只把独立成行的 `DECOMPOSE_START` / `DECOMPOSE_END` 识别为标记，避免误解析用户提示词中的说明文字；同时兼容 ChatGPT 偶尔返回的合法 JSON 代码块。

## 1.7.1

- [修复] 生图成功后使用统一的聊天读取入口进入语义拆图，修复旧函数名残留导致方向被跳过的问题；同时将进入拆图后的错误正确记录为拆图阶段。

## 1.7.0

- [调整] 每种素材类型最多进行 3 轮参考图审核，每轮固定 6 条链接；三轮后保留已通过结果并直接进入下一类型，缺失方向不再继续消耗审核请求。
- [调整] 正式流程改为 5 张弹窗、3 张 Banner、2 张浮窗；对应生图、拆图和 Figma 编号同步更新。
- [调整] 每个新参考图审核批次固定发送 6 条公开图片链接，不足 6 条的尾批继续暂存等待补齐。
- [调整] 审图等待改为每秒检查本地页面、每 15 秒最多读取一次 ChatGPT 已保存对话，并将审核提示词最小提交间隔设为 30 秒，降低频繁操作提示的触发概率。
- [新增] 检测到 ChatGPT“操作太频繁”提示时保存当前批次，自动冷却 10 分钟后回到同一聊天继续被动监听；同一批次只自动恢复一次，持续限频时停止并保留断点。

## 1.5.2

- [修复] 参考图去重不再仅凭 8×8 aHash 与宽高比淘汰图片；改用 SHA-256、稳定花瓣素材键或 aHash + 256 位 dHash 联合指纹，并记录每次重复命中的历史 Pin 与依据，避免不同弹窗因相似明暗结构被静默误判。
- [调整] 内容审核采用广义运营信号门槛：可访问、类型匹配、设计完整且出现数字、金额、券、仪表盘、趋势箭头、红包等任一信号即通过；分数和行业归属不再否决候选。

## 1.5.1

- [调整] 花瓣候选内容审核改为每批最多 5 个当日公开图片直链；只有 ChatGPT 实际访问成功且审核通过的图片才下载，避免候选审核消耗附件上传额度。
- [修复] 直链审核每 0.5 秒扫描整个聊天的最新完整标记块，并要求返回 Pin ID 与当前批次完全一致，不再依赖易变的助手消息 DOM。

## 1.5.0

- [新增] 每个方向完成预览、语义分层和复杂素材提取后立即发出 `direction_ready`，允许上一方向写入并核验 Figma 时，下一方向继续在 ChatGPT 生成和拆分。
- [新增] Figma 增量同步状态独立保存到 `figma-sync-state.json`；逐方向记录 `pending/syncing/qa_passed/failed`、节点 ID、上传数与产物 SHA-256 指纹，产物变化时自动失效旧同步且复用原节点重建。
- [调整] 全局完成命令不再接受手工汇总节点参数；仅在本地生成阶段结束、所有当前 ready 方向的产物版本均通过 Figma 视觉核验后才允许完成。
- [修复] 参考图内容审核改为每 0.5 秒监听与当前批次 Pin ID 完全匹配的 `REFERENCE_AUDIT_START` / `REFERENCE_AUDIT_END` JSON；上传和重试前优先恢复聊天中已有的完整结果，避免因消息节点或停止按钮误判而重复提交附件。

## 1.4.3

- [调整] 每个方向首次上传参考图后由 ChatGPT 在同一条消息中内部分析并直接生成预览，移除中间设计规格输出与第二次固定附件上传。
- [调整] 普通生图重试复用方向聊天中的既有参考图，仅在 ChatGPT 明确表示附件缺失时重新上传；旧版分析收据和 `spec.json` 继续兼容。
- [修复] 拆图阶段监听最新有效的 `DECOMPOSE_START` / `DECOMPOSE_END` JSON，不再因 ChatGPT DOM 选择器或停止按钮状态误判超时；恢复和重试前优先复用聊天中已生成的完整结果。
- [调整] 紧裁、贴边的小型透明素材不再因主体占比或边界比例直接丢弃，改为保留文件并在拆图报告中记录质量警告。
- [新增] 大面积复杂框架包含文字、按钮或卡片时，可在同一方向聊天中让 ChatGPT 去字并补全遮挡区域；报告明确区分源像素素材与 GPT 补全素材。
- [修复] 单个透明素材失败不再中止同方向的后续素材，部分成功方向可继续进入 Figma；失败阶段正确记录为 `transparent_assets`。

## 1.4.2

- [调整] 生图提示词保留参考图的金融主视觉类别与材质方向，不再使用 60% 相似度或固定三项变化约束。
- [调整] 移除生成后的参考图对比审计；语义拆图直接基于同一聊天中的生成预览，不再重复上传预览图。

## 1.4.1

- [修复] 恢复运行时优先复用运行记录中已验证的 ChatGPT 日期项目链接，避免同名项目导致聊天串线。

## 1.4.0

- [新增] 参考图只作为版式、配色、材质和元素类别启发，生成后增加原创度审计：整体相似度不高于 60%，至少实质调整构图、主视觉、信息结构、装饰中的三项，并重新设计活动场景、利益点、句式和数字。
- [调整] 复杂主视觉按视觉连接关系合并分组，透明素材改为直接从完整预览图的源像素抠取，保留同一主体的造型、比例、颜色、光影和细节，不再通过 ChatGPT 重绘透明素材。
- [新增] 支持 `--types popup,banner,float` 三类型隔离验证，并允许部分成功方向继续进入 Figma；弹窗可编辑侧只保留弹窗本体，Banner 和浮窗按完整画布重构。
- [修复] 新方向聊天先在当天 ChatGPT 项目中创建并验证提交结果，避免项目首页提示词未实际发送；附件上传增加可见缩略图、可移除附件和发送状态的稳定性检查。
- [调整] 参考图采集来源固定为花瓣，移除备用采集来源及其专用登录、搜索、配置和测试命令；浮窗允许金融 3D 素材、插图、红包、徽章等独立完整主体参与内容审核。
- [调整] Figma 交付只接受标记为 `native-source-pixel-matting` 且 `sourcePixelExact: true` 的透明素材，并保留左侧完整预览与右侧原生可编辑重建的双栏验收结构。

## 1.3.0

- [调整] 参考图视觉审核每批最多上传 3 张图片，降低 ChatGPT 附件额度消耗。
- [调整] 金融相关性权重提升至 50%，数字、金额符号、优惠券、仪表盘、红包和利息等元素均可作为金融线索，最低通过分调整为 60。

## 1.2.0

- [调整] 放宽参考图审核：前三项仍为硬性条件，结构与可用性改为软性信号，最低通过分调整为 70，并提高金融相关性权重至 30%。
- [调整] 移除真实品牌 Logo、必下款、百分百审批、固定收益和虚假宣传背书的全局拒绝规则。
